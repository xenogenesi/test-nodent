export default class Test {
    test(arg) {
        console.log('test with arg ' + arg);
        return 0;
    }

    /*async concurrent () {
        var [r1, r2, r3] = await Promise.all([p1, p2, p3]);
    }*/

    test2() {
        console.log('xxx');
    }
}
